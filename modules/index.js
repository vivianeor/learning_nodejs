const math = require("./math");

const a = 10;
const b = 5;

console.log(`Soma: ${math.soma(a, b)}`);
console.log(`Subtração: ${math.subtrai(a, b)}`);
console.log(`Multiplicação: ${math.multiplica(a, b)}`);
console.log(`Divisão: ${math.divide(a, b)}`);